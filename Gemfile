# frozen_string_literal: true

source 'https://rubygems.org'

# Ruby version (опционально, раскомментируйте и укажите нужную)
# ruby '~> 3.2.0'

gem 'cocoapods', '~> 1.15'
# gem 'fastlane', '~> 2.220'  # раскомментируйте при использовании Fastlane

# Sourcery (Swift codegen для моков) устанавливается через CocoaPods — см. Podfile.
# После `bundle exec pod install` бинарь: ${PODS_ROOT}/Sourcery/bin/sourcery
